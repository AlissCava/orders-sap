<mvc:View
    controllerName="orders.controller.OrderDetail"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    displayBlock="true">

    <Page title="{i18n>orderDetailTitle}" showNavButton="true" navButtonPress=".onNavBack">
        <content>
            <ObjectHeader
                title="{Cliente}"
                number="{
                    path: 'ImportoTot',
                    type: 'sap.ui.model.type.Float',
                    formatOptions: { minFractionDigits: 2, maxFractionDigits: 2 }
                }"
                numberUnit="€"
                intro="{NumOrdine}">
                <attributes>
                    <ObjectAttribute title="{i18n>lblStatus}" text="{StatoTxt}" />
                </attributes>
            </ObjectHeader>

            <Table id="lineItemsTable"
                   items="{ZET_dettagli_ordiniSet}"
                   class="sapUiResponsiveMargin"
                   width="auto">
                <headerToolbar>
                    <Toolbar>
                        <Title text="{i18n>lblOrderItems}" level="H2"/>
                    </Toolbar>
                </headerToolbar>
                <columns>
                    <Column><Text text="{i18n>colArticleCode}"/></Column>
                    <Column><Text text="{i18n>colArticleName}"/></Column>
                    <Column hAlign="End"><Text text="{i18n>colQuantity}"/></Column>
                    <Column hAlign="End"><Text text="{i18n>colPrice}"/></Column>
                </columns>
                <items>
                    <ColumnListItem>
                        <cells>
                            <Text text="{CodArticolo}" />
                            <Text text="{NomeArticolo}" />
                            <Text text="{QuantitaOrdine}" />
                            <ObjectNumber number="{Importo}" unit="€" />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </content>
    </Page>
</mvc:View>